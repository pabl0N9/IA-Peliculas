# IA-Musical (Gemini)

Nueva estructura en `agente/` con frontend Flask + Gemini (Google AI Studio).

- Ver `agente/README.md` para correr el proyecto.
- Archivos de datos: `agente/data/` (catalogo, politicas, faq).
- Lógica del agente: `agente/model/agente.py`.
- Servidor y UI: `agente/app.py`, `agente/templates/index.html`, `agente/static/`.
